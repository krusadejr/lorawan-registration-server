╔════════════════════════════════════════════════════════════════════════════════╗
║         LoRaWAN VERSION-AWARE KEY MAPPING - COMPLETE SOLUTION FLOW              ║
╚════════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────────┐
│ SCENARIO 1: USER WANTS TO CHECK THEIR LORAWAN VERSIONS                          │
└─────────────────────────────────────────────────────────────────────────────────┘

    User navigates to app
            ↓
    Clicks "LoRaWAN Version" in menu
            ↓
    GET /lorawan-version-detector
            ↓
    ┌────────────────────────────────────┐
    │ ChirpStackClient (grpc_client.py) │
    │                                    │
    │ get_device_profiles_via_rest()    │
    │   └─ REST API call to:             │
    │     GET /api/device-profiles       │
    │     Header: Grpc-Metadata-         │
    │             authorization: {...}  │
    └────────────────────────────────────┘
            ↓
    Response: [
      {
        "id": "xxx-xxx",
        "name": "Dragino WL03A",
        "mac_version": 3,          ← Enum value
        "sup_otaa": true
      },
      ...
    ]
            ↓
    ┌────────────────────────────────────┐
    │ parse_mac_version(3)               │
    │ Returns:                           │
    │ {                                  │
    │   "version": "1.0.3",              │
    │   "major": 1,                      │
    │   "minor": 0,                      │
    │   "patch": 3,                      │
    │   "is_1_0_x": True,                │
    │   "is_1_1_x": False                │
    │ }                                  │
    └────────────────────────────────────┘
            ↓
    lorawan_version_detector.html renders:
    
    ╔═════════════════════════════════════════╗
    ║ LoRaWAN Version Detector                ║
    ╠═════════════════════════════════════════╣
    ║ Profile: Dragino WL03A   │ Version: 1.0.3
    ║ Mode: OTAA Only          │ Status: ✓
    ║                                         ║
    ║ Key Mapping for 1.0.3:                 ║
    ║ • Application Key → nwk_key field      ║
    ║ • Network Key (empty)                  ║
    ╚═════════════════════════════════════════╝
            ↓
    User sees: "OK, we're using LoRaWAN 1.0.3"


┌─────────────────────────────────────────────────────────────────────────────────┐
│ SCENARIO 2: USER REGISTERS DEVICES                                              │
└─────────────────────────────────────────────────────────────────────────────────┘

    User uploads CSV:
    ┌─────────────────────────────────────────┐
    │ dev_eui    | device_profile_id | OTAA   │
    │ A84041F4.. | 8ad02259-c996... | D60F73 │
    │ ...        | ...              | ...    │
    └─────────────────────────────────────────┘
            ↓
    POST /register_devices_stream (with CSV)
            ↓
    Flask app (app.py) processes:
    1. Load CSV → extract first device_profile_id
    2. Call ChirpStackClient.get_lorawan_version_from_profile_id()
            ↓
    ┌────────────────────────────────────────────┐
    │ get_lorawan_version_from_profile_id()      │
    │                                            │
    │ • Calls get_device_profiles_via_rest()    │
    │ • Filters by device_profile_id            │
    │ • Returns version dict for that profile   │
    │                                            │
    │ Returns:                                   │
    │ {                                          │
    │   "version": "1.0.3",                      │
    │   "is_1_0_x": True,         ← KEY INFO!   │
    │   "is_1_1_x": False,                       │
    │   "name": "Dragino WL03A",                 │
    │   "supports_otaa": True                    │
    │ }                                          │
    └────────────────────────────────────────────┘
            ↓
    Send status to user:
    "LoRaWAN-Version erkannt: 1.0.3"
            ↓
    For each device in CSV:
            ↓
    ┌──────────────────────────────────────────────┐
    │ create_device_keys(                          │
    │   dev_eui="A84041F4935D6EEA",               │
    │   nwk_key="E119...",                         │
    │   app_key="D60F739062E3B90B...",             │
    │   lorawan_version={                          │
    │     "is_1_0_x": True,    ← VERSION KNOWN!   │
    │     "is_1_1_x": False,                       │
    │     ...                                      │
    │   }                                          │
    │ )                                            │
    └──────────────────────────────────────────────┘
            ↓
    ┌──────────────────────────────────────────────┐
    │ Key Mapping Logic:                           │
    │                                              │
    │ if lorawan_version:                          │
    │   if lorawan_version['is_1_0_x']:            │
    │     # LoRaWAN 1.0.3 semantics               │
    │     DeviceKeys.nwk_key = app_key  ← CORRECT!│
    │     DeviceKeys.app_key = ""        ← BLANK  │
    │                                              │
    │   elif lorawan_version['is_1_1_x']:          │
    │     # LoRaWAN 1.1.0 semantics               │
    │     DeviceKeys.nwk_key = nwk_key            │
    │     DeviceKeys.app_key = app_key            │
    │                                              │
    │ else:                                        │
    │   # Fallback (backward compatible)           │
    │   use old is_otaa logic                      │
    └──────────────────────────────────────────────┘
            ↓
    Create gRPC protobuf message:
    DeviceKeys {
      dev_eui: "A84041F4935D6EEA"
      nwk_key: "D60F739062E3B90B..."  ← AppKey here for 1.0.x!
      app_key: ""
      gen_app_key: ""
    }
            ↓
    Send to ChirpStack via gRPC
            ↓
    ChirpStack stores:
    Device: A84041F4935D6EEA
      ├─ Network Key: D60F739062E3B90B...  ✓ CORRECT
      └─ App Key: (empty/unused)           ✓ EXPECTED
            ↓
    User checks in ChirpStack UI:
    "Yes! The Application Key value is D60F739062E3B90B..."
                              ✅ SUCCESS!


┌─────────────────────────────────────────────────────────────────────────────────┐
│ KEY DIFFERENCES: 1.0.x vs 1.1.x SEMANTICS (THE REAL PROBLEM!)                   │
└─────────────────────────────────────────────────────────────────────────────────┘

LoRaWAN 1.0.3 (e.g., Dragino, IMEI devices):
┌─────────────────────────────────────────┐
│ CSV Column        → Protobuf Field      │
├─────────────────────────────────────────┤
│ OTAA keys        → DeviceKeys.nwk_key   │✓ CORRECT
│ (network key col) → ignored             │
└─────────────────────────────────────────┘

LoRaWAN 1.1.0 (newer devices, future standard):
┌─────────────────────────────────────────┐
│ CSV Column            → Protobuf Field  │
├─────────────────────────────────────────┤
│ lora_nwkskey         → DeviceKeys.nwk_key│
│ lora_appkey          → DeviceKeys.app_key│✓ STANDARD
└─────────────────────────────────────────┘

⚠️  PROBLEM: Field names don't match their meanings!
    - nwk_key = "Network Key" in name
    - nwk_key = "Application Key" in reality (for 1.0.x)
    - Silent failure if you send wrong version!

✅  SOLUTION: Auto-detect version from device profile,
    route keys to correct fields accordingly.


┌─────────────────────────────────────────────────────────────────────────────────┐
│ ERROR HANDLING & FALLBACK                                                       │
└─────────────────────────────────────────────────────────────────────────────────┘

Version Detection Fails
    ↓
    ┌─────────────────────────────────┐
    │ Graceful Fallback:              │
    │ • Log warning                   │
    │ • Fall back to is_otaa flag     │ ← Old bandaid logic
    │ • Registration continues        │
    │ • Alert user in UI              │
    └─────────────────────────────────┘
    ↓
Registration proceeds (may not be perfect, but won't crash)

REST API Timeout
    ↓
    ┌─────────────────────────────────┐
    │ • Retry up to 3 times           │
    │ • Log detailed error            │
    │ • Fall back to OTAA detection   │
    │ • Don't block registration      │
    └─────────────────────────────────┘
    ↓
Registration continues


┌─────────────────────────────────────────────────────────────────────────────────┐
│ PERFORMANCE CONSIDERATIONS                                                      │
└─────────────────────────────────────────────────────────────────────────────────┘

Per Registration Batch:
  • REST API call: 1 call (all profiles, cached by ChirpStack)
  • Response size: ~1-5KB depending on number of profiles
  • Response time: 100-300ms typical
  • Parsing: <1ms
  • Total overhead: ~200ms per batch (negligible)
  
vs.

Per Device Registration:
  • gRPC protobuf message: ~100 bytes
  • ChirpStack processing: 50-200ms per device
  • Total overhead insignificant


┌─────────────────────────────────────────────────────────────────────────────────┐
│ BRANCH STRUCTURE                                                                │
└─────────────────────────────────────────────────────────────────────────────────┘

        main (original code with bandaid OTAA detection)
          │
          └─→ feature/lorawan-version-detection ← YOU ARE HERE
                ├─ Commit 1: Code implementation (grpc_client.py, app.py, templates)
                ├─ Commit 2: Documentation files
                ├─ Commit 3: Feature overview
                └─ Commit 4: Solution summary

When ready to deploy:
  • Test on feature branch
  • git checkout main
  • git merge feature/lorawan-version-detection
  • Deploy to production


┌─────────────────────────────────────────────────────────────────────────────────┐
│ TESTING WORKFLOW                                                                │
└─────────────────────────────────────────────────────────────────────────────────┘

1. Start server on feature branch
   ↓
2. Visit /lorawan-version-detector
   ├─ See your device profiles
   ├─ See their versions (1.0.3, 1.1.0, etc.)
   └─ Note: "Dragino WL03A" = 1.0.3
   ↓
3. Upload CSV with test device
   ├─ First device profile should be the one you saw in step 2
   ├─ Watch logs for: "Detected LoRaWAN version: 1.0.3"
   └─ Complete registration
   ↓
4. Check ChirpStack
   ├─ Open registered device
   ├─ Click Edit → Device Keys
   ├─ Look at "Network Key" field
   └─ Should show the value from your CSV "OTAA keys" column ✓
   ↓
5. Success! 🎉


═══════════════════════════════════════════════════════════════════════════════════
                                  KEY METRICS

Before (Bandaid):        After (Proper Solution):
❌ Guesses OTAA          ✅ Queries actual version
❌ JSON column required  ✅ Works with any device
❌ No diagnostics        ✅ Full diagnostic page
❌ May fail silently     ✅ Shows detected version
❌ No future-proof       ✅ Scales to 1.2.x, 1.3.x, etc.

═══════════════════════════════════════════════════════════════════════════════════
